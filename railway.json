{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "DISABLE_ESLINT_PLUGIN=true npm install && DISABLE_ESLINT_PLUGIN=true npm run build"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}